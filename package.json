{
  "name": "lanternfall",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A four-floor turn-based fantasy roguelike for phones and browsers.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:balance": "node scripts/simulate.mjs"
  },
  "engines": { "node": ">=22" }
}
